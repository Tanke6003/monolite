node_modules/
dist/
*.tsbuildinfo
coverage/

# Real environment files never get committed; the templates that document them do.
.env
.env.*
!.env.template
!.env.example

# Scratch output from running the CLI by hand during development.
/tmp/

# Projects the scaffold test generates and compiles. Inside the repository so
# they can resolve its node_modules; removed when the test finishes.
/.scaffold-tests/
/.wizard-tests/

